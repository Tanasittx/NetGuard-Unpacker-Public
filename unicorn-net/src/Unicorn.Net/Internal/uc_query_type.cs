namespace Unicorn.Internal
{
    internal enum uc_query_type
    {
        // From unicorn.h

        // Dynamically query current hardware mode.
        UC_QUERY_MODE = 1,
        UC_QUERY_PAGE_SIZE,
    }
}
